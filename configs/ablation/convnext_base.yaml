# @package _global_
# Semantic backbone capacity: ConvNeXt-Base in place of ConvNeXt-XXL.
#
#   uv run torchrun --standalone --nproc_per_node=2 train.py \
#       ablation=convnext_base trainer.out_dir=runs/convnext_base

model:
  convnext_model: convnext_base.clip_laion2b
