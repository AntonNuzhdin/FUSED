# Per-token MoE expert utilization per branch and generator
#   uv run python eval/routing_utilization.py checkpoint=weights/fused_opensdi.pth
defaults:
  - base
  - _self_

test_json: data/opensdi/test.json
out_dir: results/routing

plot: true
plot_only: false
